# Short Response Questions
1)  ***If you are collborating with a classmate what is the first thing that each of the team members need to do? Why is it important to do this step?***

If you are collaborating with a classmate, the first thing that each of the team members needs to do *is set up the same repository and giving access to the collaborator*. This step is an important step because it grants access to the collaborator, makes a repository and it also is needed to your work. 

---
2) ***Why do developers use branches?***

Developers use branches to work on changes without changing the main code. It helps them keep the code organized.

---
2) ***What is git? What is github? How does git and github work together?***
Git helps you save your work and view all changes. GitHub is a website to store and share your work. Together,they let you work safely on your projects and share them easily as git sends your saved work to GitHub.

---
3) ***What is wrong with te following command sequence? What should be the correct cimmand sequence?***

`git commit -m "saving work"`
 
 `git add`
 
 `git push`

 The error in the command sequence is that the person saved their work in the wrong order as the correct sequence should be :


`git add`

`git commit -m "saving work"`
 
 `git push`

 This would properly save your work properly as it is in the correct order.
 ---